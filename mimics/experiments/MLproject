name: mimics

entry_points:
    brows_alpha:
    parameters:
        cv: {type: float, default: 5}
        n_jobs: {type: float, default: 1}
        verbose: {type: float, default: 0}
    command: 'python commands.py brows_alpha {cv} {n_jobs} {verbose}'
